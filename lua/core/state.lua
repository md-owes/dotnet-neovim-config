-- This module holds state that needs to be shared across
-- different parts of the configuration without using global variables.

return {
	-- Initialize the value for our last-used tab feature
	last_tab = 1,
}
